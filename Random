local originalLoadstring = loadstring
function loadstring(source)
    if source:find("raw.githubusercontent.com/ZaRdoOx/Fazium-files/main/Random") then
        print("test ok")
    end
    return originalLoadstring(source)
end

_G.LoaderValidated = true
